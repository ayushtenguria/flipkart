import initDB from "../../extra/initDB";
import product from "../../model/product";

initDB()

// export default (res, req) => {
//   product.find().then((product) => {
//     res.json(products);
//   });
// };
